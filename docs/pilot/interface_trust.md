# Pilot Interface Trust And Visual Cohesion

Related: [Pilot_todo](../pilot_todo.md) | [README](../../README.md) | [AGENTS](../../AGENTS.md) | [MVP to-do list](../mvp_todo.md) | [Decision log](../decision_log.md) | [Work sessions log](../work_sessions.md)

## Purpose

Interface polish, visual trust, copy, public/auth/student shell evidence, and remaining walkthrough risks.

This file holds long-form evidence and historical status notes. Keep the canonical task IDs and checkboxes in [Pilot_todo](../pilot_todo.md).

## Task IDs

- P1.1 - Audit the shared visual language across public shell, app shell, cards, forms, and settings surfaces.
- P1.2 - Define a calmer brand system for colors, typography, spacing, states, and empty/loading/error views.
- P1.3 - Remove distracting UI imperfections and obvious polish gaps on the highest-traffic routes.

## Evidence And Status Notes

Status note: the 2026-08-29 tablet polish pass keeps the public wordmark readable by moving the audience selector onto its own row below desktop width, gives pricing a localized page heading and orientation copy, and removes English-only tooltip or overlay labels from localized public and student shells. French and Chinese public-route screenshots plus the new public localization smoke provide before/after evidence; `P1.3` remains open for authenticated learner walkthrough feedback and broader visual cleanup.
Status note: the shared shell and primitive layer still owns the theme bootstrap through `app/globals.css`, `components/theme/theme-script.tsx`, and `lib/theme/config.ts`, and the MVP now exposes only a simple light-or-dark toggle in the shared public/app toolbar chrome while leaving the earlier preset/custom theme controls hidden. The landing page now also uses a wider story-first product layout with a simplified near-full-width top bar, a centered hero, three alternating `preview + 3 glass cards` rows with matched GIF sizing, locally hosted neutral placeholder GIFs in `public/landing/`, a shared slow CTA gradient drift, a floating helper for links plus pilot context, a globe-menu language dropdown, and a centered closing CTA instead of the previous toolbox hero. The shared auth route now also uses a smaller HUD-style header, drops the repeated footer framing, stays as one centered card by default, and starts directly with the `Sign in` or `New user` segmented control instead of carrying explanatory copy above it. The authenticated shell also drops pilot badges and literal route-hint scaffolding from the main role surfaces, while reusing the same utility controls in its top header. `P1` stays open because real product media, stronger copy iteration, empty/loading/error consistency, and broader route-level visual cleanup still remain.
Status note: the visible filler copy sweep now also replaces the most implementation-facing language across pricing, onboarding, auth-complete, app-home, landing helper text, and the shared student start/history/workbench surfaces, so those routes present themselves as product flows rather than scaffolding. `P1` still remains open because the remaining polish work is now more about copy excellence, media quality, and route-level coherence than obvious placeholder leaks.
Status note: the parent `/app` dashboard now has a role-specific oversight layout with a compact account or billing dock, a linked-learners rail, and one grouped activity hub for weekly rhythm plus recent sessions, replacing the more generic shared-sidebar plus scattered-card pattern. `P1` still stays open because the new layout still needs real parent walkthrough feedback, final spacing or hierarchy polish, and stronger visual consistency with the tutor and student surfaces.
Status note: the student role now also uses a dedicated `StudentAppShell` with a collapsible subject rail, a minimal top bar, a placeholder learner avatar dock, and a flatter chat-first conversation layout with a secondary sources/notes rail. `/app/new` no longer acts as a learner destination and now only survives as a compatibility redirect. `P1` remains open because the student shell still needs real walkthrough polish, the avatar is still placeholder-only, and `maps` or `tests` remain explicit placeholders rather than real tools.
Status note: the student shell has now been tightened again around that chat-first model: subject lists and recent homework chats are flatter and more list-like, the learner profile dock defaults to a minimal unoutlined identity row with a hover menu, the subject view and live conversation route both use a true right-side panel instead of floating meta cards, and the quick-start/live composer controls now sit inside the input field with discreet `+`, disabled mic, and send actions. `P1` still remains open because the student shell still needs real learner walkthrough evidence, a real avatar flow, and placeholder `Maps`/`Tests` content still has to either become real tools or disappear before a broader rollout.
Status note: the live conversation route now opts out of the generic centered authenticated-shell canvas and the workbench no longer uses negative/right-padding rail compensation. The chat article keeps its own symmetric inner padding for composer and transcript breathing room, and the globally reserved scrollbar gutter is disabled on live conversation routes because those routes use internal scroll areas.
Status note: the subject view now treats the `History` tab as the only place for recent subject chats and no longer repeats the selected subject as a second large page title below the persistent app header. The left homework rail also gets an immediate client-side conversation upsert after subject quick-start creates a new homework shell, so the subject counter can update before the parent app layout is naturally refreshed.
Status note: the empty-state homework launcher now uses full-width two-line guidance instead of a capped prose block, and the student quick-start textarea chrome is stripped down to a flatter field without the leftover inner browser focus box. `P1` still remains open because the broader student shell still needs more walkthrough polish and real-device evidence.
Status note: the student chat inputs now also accept pasted clipboard images through the existing private upload path, and learner-visible provider failures no longer dump the old internal `Coach brouillon` text into the transcript. `P1` still remains open because the broader student shell still needs more walkthrough polish, stronger provider reliability, and real-device evidence.
Status note: the student shell now also carries a placeholder `Forward` activity alongside `Maps` and `Tests`, reserved for future "what comes next" guidance at a high level. It remains intentionally non-functional for now, and `P1` still stays open because these placeholder learning tools need to become real, be redesigned, or be removed before broader rollout.
Status note: for the student demo, those not-implemented sidebar "later" sections should not behave like normal clickable destinations. Disable, hide, or clearly mark placeholder-only areas such as `Forward`, `Maps`, and `Tests` until they either become real tools or are intentionally removed from the student shell.
Status note: the live student conversation rail is now reduced to a minimal file strip plus one explicit completion button at the bottom. The old learner-facing attachment detail card and session-summary panel are gone from the active chat, while file removal is now possible directly from uploaded-file pills. `P1` still remains open because the remaining chat surface still needs broader walkthrough evidence and a later decision on whether explicit completion should stay visible at all.
Status note: the student conversation rail is now also pinned as a viewport-height split pane under the student header instead of stretching through the whole transcript, and uploaded image pills can open a full overlay preview without leaving the chat. `P1` still remains open because the broader chat surface still needs real learner walkthrough evidence and a later decision on whether explicit completion should stay visible at all.
Status note: the student conversation rail now previews uploaded images inline before any fullscreen expansion, adds a hover-only expand action for the larger overlay, and lets desktop users resize the rail width manually. `P1` still remains open because the broader chat surface still needs real learner walkthrough evidence, plus a later decision on whether private-image preview should gain generated thumbnails for faster loads.
Status note: the student transcript now also keeps hover-only copy controls under each turn and renders assistant math through a KaTeX-backed markdown path, so basic formula replies no longer leak raw `$...$` syntax into the learner view. `P1` still remains open because the broader chat surface still needs real learner walkthrough evidence, richer message rendering QA, and a later decision on whether explicit completion should stay visible at all.
Status note: the live student composer now stays pinned at the bottom of the conversation view, and a chevron jump-to-latest control appears above it when the learner scrolls up through older turns. `P1` still remains open because the broader chat surface still needs real learner walkthrough evidence and more cross-device behavior checks around long transcripts.
Status note: deploys or browser reloads during an active homework conversation should not make banban start over if the route still points at the same persisted conversation. Already-saved messages, workspace state, and attachments should reload from durable state. If the learner can see previous turns but the next coach reply behaves like a fresh chat, investigate prompt context assembly, message loading, or the bounded recent-transcript window instead of treating it as acceptable deploy behavior. A single in-flight request lost during a deployment remains a retry/draft-recovery problem.
Status note: the explicit completion contract now surfaces its result immediately again in the live chat. After `Homework done!`, the right rail opens a fourth `Summary` section with the returned student recap, weakness tags, and next-step recommendation instead of leaving completion visible only in backend state or after a later page reload. `P1` still remains open because the broader chat surface still needs real learner walkthrough evidence and a later decision on whether the explicit completion button itself should remain permanent.
Status note: the completion control now also asks for explicit confirmation before locking a homework session, and the student-side deterministic fallback summary no longer dumps internal metadata such as transcript counts, file bookkeeping, or hidden workspace-state fields. `P1` still remains open because the broader chat surface still needs real learner walkthrough evidence and further qualitative tuning of the provider-backed recap itself.
Status note: the live homework upload path now hardens one frequent trust break on heavier files: the student workbench preflights count and total-byte limits against files already attached to the conversation, resolves ordinary attachment types from filename extensions when browser MIME metadata is missing or odd, and surfaces field-level localized upload validation errors instead of collapsing back to the generic `Un ou plusieurs champs sont invalides.` wrapper. `P1` still remains open because the chat surface still lacks a true upload or extraction progress meter and broader real-device evidence on slow-network or heavier-file behavior.
Status note: the live composer now also shows a tiered progress ring in place of the send icon while attachment work is active, advancing through `prepare -> upload -> extract` without pretending to know byte-level transfer progress. `P1` still remains open because the workbench still lacks a true byte-accurate upload bar for heavier files and broader real-device evidence on slower networks.
Status note: the subject quick-start on `/app?view=homework&subject=...` now reuses that same ring language for its own staged-file handoff, so the learner no longer sees one loading idiom on the dashboard and a different one in the live chat. The subject-page version only represents the initial `prepare/open chat` stage, because the actual file upload and extraction still happen after the route transition into the conversation surface.
Status note: the subject quick-start now also front-loads the real upload and extraction pass before the first learner prompt is auto-sent, and if attachment prep fails it opens the conversation with the draft restored but auto-send paused. This closes one of the most visible trust breaks on heavier files: the learner no longer spends the first prompt only to discover afterward that the attached source never became readable context.
Status note: the attachment handoff now also clamps extracted-text preview sync to the same workspace character limits already enforced by the server, so a valid PDF can finish extraction and stay attached without the follow-up hidden workspace save collapsing into a generic `Un ou plusieurs champs sont invalides.` error. The full extraction still lives on the attachment record; only the workspace mirror is bounded.
Status note: if that first extraction attempt fails because Gemini is temporarily unavailable but the conversation workbench's automatic retry succeeds moments later, the restored first prompt now resumes automatically as long as the learner has not edited it. This keeps the demo flow from stalling on an obsolete "text could not be extracted reliably" banner after the attachment has actually become readable.
Status note: the left student rail keeps the Dashboard row, then shows the enabled Homework/Devoirs section followed by disabled placeholder rows for Recap, Exams/Tests, Forward/Poursuivre, and Explore/Explorer. The root Homework `+` is removed, per-subject `+` controls are replaced by chevrons that expand the latest five conversation titles with `...` when more exist, only one subject subsection expands at a time, Forward uses a rocket icon, dividers separate Homework and Tests from later sections, disabled rows use a custom hourglass cursor plus `Coming soon!` tooltip, and Homework/subject chevrons share the same right action column. Disabled placeholder labels now align with their icons, while descriptions sit in a reserved second line that fades in on hover without changing row height; the future-tool cluster keeps tighter internal spacing than the main navigation stack.
Status note: the Gemini adapter now also separates retry budgets by operation and adds jittered exponential backoff instead of the old uniform short sleep. Attachment extraction still retries more patiently than chat, while `MAX_TOKENS` on suspiciously truncated replies no longer triggers extra same-model retries that only add load without fixing the underlying cap or prompt-shape issue.
Status note: the retired `/app/new` path exposed one more student dead end after the route cleanup: brand-new learners with no existing subject tags landed on the homework dashboard but only saw a self-link CTA. The dashboard empty state now owns a real first-homework launcher with subject selection, but `P2.1` still remains open because the broader student first-run path still needs full walkthrough validation after the onboarding and homework-start changes.
Status note: add a focused new-user dashboard follow-up under `P2.1`: the learner home for a freshly created account should feel purpose-built for first homework start instead of looking like the returning-user homework dashboard with only partial empty-state adaptation.
Status note: add a focused auth-surface polish follow-up under `P1.3` for the first commercial-feeling student demo. Sign-up and onboarding now share a quieter HUD-style header, remove footer/explainer microcopy, and make onboarding more direct, but the full first-account walkthrough still needs real-user validation before `P1.3` can close.
Status note: the student Dashboard route is now distinct from the Homework route. `/app` defaults to a product-facing overview headed by the centered title `Where should we start?` with a hard-working emoji marker, with no redundant subtitle, then a wide Homework row, matching left-rail icons, and subject links that wrap into natural-width rows, followed by one row for Recap/Exams/Forward/Explore coming-soon cards and a second-row `Level up!` placeholder for future cross-subject practice recommendations; subject-specific launch and resources stay in the Homework view. The dashboard subject chips now link to their subject page and use grey for no homework yet, yellow with an active-session counter when homework is still open, and green when all visible sessions in that subject are completed.
Status note: the root Homework view now avoids a misleading first-subject default. It enters with every subject shortcut unselected, shows a disabled composer until a subject is chosen, shows homework items across all subjects ordered by latest visible activity, and uses a compact vertical-ellipsis editor for browser-local subject shortcut preferences: default-subject visibility, drag-enabled ordering, and add/edit/delete custom shortcut labels. Subject shortcut clicks on that root view now update the title, locally filter the quick-start composer and homework list, and write `/app?view=homework&subject=...` with `window.history.replaceState` instead of forcing a route transition. Left-rail subject clicks are also intercepted when the learner is already on the Homework view, with optimistic rail highlighting and the same local Homework launcher event; links from Dashboard or conversations still navigate normally. Direct links and refreshes on the same URL hydrate the same Homework launcher with that subject selected. Subject-resource libraries are preloaded only for the URL-selected subject and then lazy-loaded on first local subject selection, instead of loading every subject's resources on every Homework render. The Homework rail and root subject chips count only active unfinished homework, while completed homework stays visible with compact status pills without inflating counters. This is intentionally not a canonical subject-entity model yet.
Status note: add a demo-data utility follow-up under `P1.3`: provide a safe dev/admin-only way to populate any test account with homework conversations by subject and status (`active`, `completed`, and optionally `archived`) so Dashboard, left-rail counters, root Homework chips, and per-subject Homework lists can be tested without hand-creating many sessions. The utility should be explicit about target account, subject tags, counts, and whether existing demo homework should be preserved or replaced.
Status note: the public landing route now has the first identity-aware revamp from archived `docs/archive/public_landing_page_revamp_brief.md`: a full-width scroll-aware navbar with the Student/Parent/Tutor selector in the center, no floating helper on the first-time visitor page, Parent as the default reassurance-heavy first view, and a shared feature-section template with a centered title, large GIF/demo placeholder, alternating media/cards order, and three adjacent cards. The first Parent feature is the documented exception: a two-card generic-AI-versus-banban comparison with a full-width demo GIF. Parent feature 2 now uses a static dashboard overview image asset, and Parent features 3 and 4 reuse a static conversation workspace image asset, with the media frame accepting non-GIF images. Pricing moved out of the center nav into a compact hover-centered `?` menu beside the theme toggle, with a temporary FAQ placeholder, and `/pricing` now uses the same landing-style header plus public-facing identity-specific plan cards instead of pilot/internal framing or hero explainer copy. Tutor selection is temporarily disabled with a tooltip/toast on both landing and pricing until tutor accounts are implemented, so Tutor pricing cards stay hidden for now. The hero is now centered, no longer renders the role eyebrow, no longer carries the placeholder `banban workspace` mock, and parent/tutor oversight details open from inline subtitle links into a large overlay. Parent uses the default feature template for workspace, BigBrain, and learning modes; Student feature content intentionally remains empty until copy is written and hides the closing CTA block. `P1.3` remains open because final student/tutor copy, real product media, and broader public-page conversion review still need iteration.
